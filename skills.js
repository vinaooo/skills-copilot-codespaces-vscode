function calculateNumbers(var1, var2) {
    return var1 + var2;
}

module.exports = calculateNumbers;
// This function takes two numbers as input and returns their sum.
// It can be used in other files by importing this module.
// Example usage:
// const calculateNumbers = require('./skills.js');
// console.log(calculateNumbers(5, 10)); // Outputs: 15